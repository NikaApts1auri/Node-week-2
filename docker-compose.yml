version: '3.4'

services:
  nodewk2:
    image: nodewk2
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
